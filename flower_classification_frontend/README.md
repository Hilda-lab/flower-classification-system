# 五类花卉识别前端

基于 Vue 3、Vite 和 Vue Router 的花卉识别界面，包含首页花卉百科、图片/摄像头识别、历史记录、用户中心和管理员统计页面。

支持雏菊、蒲公英、玫瑰、向日葵、郁金香五类花卉，标签顺序与后端模型一致。

## 本地开发

```bash
npm install
npm run dev
```

前端默认访问 `http://127.0.0.1:3000`，后端 API 默认由 `http://127.0.0.1:5001` 提供。

## 构建

```bash
npm run build
```
